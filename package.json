{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "ver": "gh-pages -d docs/_site --nojekyll --cname erstes-wiki-doc.ahrensburg.city"
  }
}